name: esc_pos_printer
description: The library allows to print receipts using an ESC/POS thermal WiFi printer.
version: 4.0.3
homepage: https://github.com/andrey-ushakov/esc_pos_printer

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^3.0.2
  esc_pos_utils: 
    path: ../esc_pos_utils/
  flutter_usb_printer:
    git:
      url: https://github.com/Noel-S/flutter_usb_printer.git
  flutter_bluetooth_basic:
    path: ../flutter_bluetooth_basic/

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
